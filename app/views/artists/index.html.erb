<h1 class="ui header">Artists</h1>
<hr />
<ul>
    <% @artists.each do |a| %>
        <li><%= link_to a.name, billboard_path(a) %></li>
    <% end %>
</ul>

<h1 class="ui header">Artists</h1>

<% @artists.each do |art| %>
    <%= link_to art.name, artist_songs_path(:artist_id)%>
<% end %>