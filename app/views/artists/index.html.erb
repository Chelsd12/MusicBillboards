<h1 class="ui header">Artists</h1>
<hr />
<ul>
    <% @artists.each do |a| %>
        <li><%= link_to a.name, artist_songs_path(a) %></li>
    <% end %>
</ul>
<%= link_to "Add new artist", new_artist_path, class: "ui button" %>